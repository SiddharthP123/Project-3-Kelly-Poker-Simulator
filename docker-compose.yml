services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: kelly_user
      POSTGRES_PASSWORD: kelly_password
      POSTGRES_DB: kelly_poker
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
