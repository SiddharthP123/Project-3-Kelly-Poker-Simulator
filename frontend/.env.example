# Local dev default. In Vercel, set VITE_API_BASE_URL as a project env var
# instead, pointing at the deployed Render backend, e.g.:
#   https://kelly-poker-backend.onrender.com/api
# See ../README.md "Part 11: Deployment" for the full runbook.
VITE_API_BASE_URL=http://localhost:8000/api
